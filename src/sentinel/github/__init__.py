"""Everything that talks to GitHub: the REST client Sentinel calls out with.

Nothing is re-exported here. Import from the module that owns it — `sentinel.github.client` for
the API client — so that adding a module to this package never means editing a file another task
also holds.
"""
