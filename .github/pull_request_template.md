Closes #

## What changed

<!-- What this does, and why. Link the spec section it implements. -->

## Testing

<!-- What the tests cover, and how to run them. For UI work, name the component tests. -->

## Decisions

<!-- Link any ADR added, or write "none — nothing non-obvious was decided". -->

---

- [ ] Implemented as specified in `docs/`
- [ ] Unit tests added for everything changed; `make test` green
- [ ] UI changes have component tests (browser-level tests are out of scope)
- [ ] `ruff` and `mypy` pass
- [ ] No files changed outside this task's owned files in `docs/tasks.yaml`
- [ ] `/finish-task` run: review applied, no findings remaining
- [ ] Non-obvious decisions recorded in `docs/adr/`; `make adr-index` re-run
- [ ] Spec updated if the implementation diverged from it

<!-- Anything left undone or knowingly deferred: say it here rather than leaving it implied. -->
